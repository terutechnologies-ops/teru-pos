services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-su_arepa}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-su_arepa}
      POSTGRES_DB: ${POSTGRES_DB:-su_arepa_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-su_arepa}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db_data:
